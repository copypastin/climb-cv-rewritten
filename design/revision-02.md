# Revision 02 — outcomes against `reviews/guardian-02.md`

Date: 2026-08-07. Scope was deliberately limited to guardian-02's **two blocking findings**; findings 3–15 are recorded below as DEFERRED, not dropped. Guardian-02 stated that fixing 1 and 2 makes the surfaces lockable and that no third full review is warranted.

Applied directly in the main session rather than by `framework-core`, which hit the account spend limit mid-run. Its verification script survived in the scratchpad and became the acceptance test.

## Blocking — both closed

| # | Finding | Outcome | Where |
|---|---------|---------|-------|
| **1** | Read-only / lifetime array guarantee does not survive a pickle round-trip. `__setstate__` on the `_Arrays` mixin is shadowed by the one `@dataclass(slots=True)` installs (CPython `_add_slots` guards on `cls_dict`; an inherited method is never in `cls_dict`). Plus `base is not None` is the wrong ownership test, and `Record.data`'s nested arrays are unreachable from `_ARRAY_FIELDS`. | **ADDRESSED — verified by execution** | `payloads.md` §2.2.1 rewritten: `@payload` decorator applied *outside* `@dataclass`, wrapping the installed `__setstate__` rather than reimplementing it so it cannot drift with CPython; `flags["OWNDATA"]` + `np.array(arr, copy=True, order="C")`; `_TREE_FIELDS` walking `Record.data` recursively. Acceptance test at `design/verify/payload_immutability.py`, **all checks passing on Python 3.13.11 / numpy 2.4.1** — round-trip read-only for all 7 contract types, aliasing-view race closed, mutation raises, nested `Record` arrays read-only at every depth. §2.2.1 now instructs the reader to run it before trusting the section. |
| **2** | `Record` has no declared, checked `kind`, so the type that opens the payload vocabulary is the one payload with no wiring-time agreement — `schema` is `record/1` for every record topic. | **ADDRESSED** | Three enforcement points: `loader.md` §3.1 (`record_kind` required when `payload = "record"`, forbidden otherwise, plus a manifest example); `broker.md` §2 (`TopicDescriptor.record_kind`, joined to §4.2's merge-equality check so §4.3's contradiction error covers it with no new message text); `payloads.md` §3.6 (the three-place rule, the `grip_viz`/`acme.hand_state` failure it closes, and why v1 is the only opportunity). Also `broker.md` §7: `climbcv topics -v` now prints payload type, `schema`, and `unit`/`record_kind`. |

## Deferred — recoverable by additive change, none blocks implementation

Guardian-02's own framing: "everything in findings 3 through 15 is recoverable by an additive change and I would not hold implementation for any of them — though 3, 4 and 5 will each cost someone a bad afternoon."

| # | Finding | Severity | Status and what deferring costs |
|---|---------|----------|--------------------------------|
| **3** | `provides_topology` cannot express "same topology as my input", so a topology-agnostic transform stage has no honest value to declare | should-fix (high) | **DEFERRED.** Highest-consequence deferral: installing a third-party `coco.17` pose plugin quarantines the first-party `core.smooth_oneeuro` as a contract violator and, as exclusive publisher of `pose.smoothed` with required subscribers, shuts the app down. Until fixed, "capture and pose are both swappable" (Assumption §3, marked *Confirmed*) holds only for plugins providing the one topology the first-party stages were built around. `payloads.md` §4.1 already prescribes the fix in prose while the schema forbids expressing it. Cheap form: let `provides_topology` accept a list. |
| **4** | `self.stopping` cannot change while the blocking handler it exists for is running — the child is single-threaded, so nothing can assign to it mid-handler | should-fix (high) | **DEFERRED.** S18's fix is currently in place and inert: the blessed `@every(0)` source idiom never sees `stopping` flip, so an IP-camera plugin is terminated after `grace_s`, `teardown()` never runs, the handle leaks. Fix is invisible to authors (make it a property over the shutdown `Event`), so deferring is cheap *now* and expensive after v1 ships, when authors have built on the observable behaviour. |
| **5** | §7.4 tells embedders shared mutable state is normal without saying they are on a different thread — and today's `start(blocking=True)` runs `on_landmarks` on the caller's thread | should-fix (high) | **DEFERRED.** Includes an unflagged migration break: an existing embedder whose callback touches Tk or `cv2.imshow` works today and gets undefined behaviour after the refactor. Also the one asymmetry left standing — a GUI-owning *plugin* gets a named idiom (§3.8); a GUI-owning *host* gets a sentence pointing the wrong way. |
| **6** | §7.6's always-fatal unknown-topic rule keys off the *enabled* plugin set, so a user disabling an optional plugin can stop the app | should-fix | **DEFERRED.** Reachable by ordinary user config in every shipped embedding app, and the failure lands on the user. `broker.md` §4.4 already made this exact relaxation for plugins. Cheapest before §7 has an installed base. |
| **7** | Host `required` defaults to `False` — the one direction `payloads.md` §5 forbids reversing | should-fix | **DEFERRED.** A headless embedder is the primary embedding shape and nothing but the host requires `pose.smoothed`; disable the smoother and the app runs forever producing no callbacks with one INFO line. Recommendation was a mandatory keyword, mirroring §7.3's `requires_topology`. Cheapest before §7 has an installed base. |
| **8** | No programmatic framework config — `ClimbCV.publish()` ships unusable, since disabling the competing `core.capture` requires a TOML file the embedder may not own | should-fix | **DEFERRED.** `config-contract.md` §1 already states the framework receives a plain dict, so accepting `config=<dict>` is nearly free. Cheapest before §7 has an installed base. |
| **9** | The shared-topic `latest()` warning fires on deployment shape, not declared contract, so the author never sees it | should-fix | **DEFERRED.** B1's failure with diagnostics one machine too late: the warning fires at the user's site naming a plugin they cannot fix. Exclusivity is statically known before any process starts. Would also earn `mode = "latest"` its keep as a permanent manifest key. |
| **10** | Unknown manifest keys are silently ignored | should-fix | **DEFERRED.** A typo defeats fixes that each cost a review round: `conflat = false` reinstates F-14, `platform = [...]` reinstates the mac-only crash loop, `teardown_timeout` reinstates S8's lost session. `config-contract.md` §3.1 refuses this exact behaviour for `[framework]` keys; the mechanism exists and just needs reusing. |
| **11** | `app.status`'s exclusivity is never stated — the only standard topic missing from the exclusivity table | should-fix | **DEFERRED.** Whichever way it is implemented becomes the contract by accident, and exclusive→shared is breaking by `broker.md` §3.1's own argument. One table row. |
| **12** | S9's deferral is sound, but `holds.boxes` cannot use the offered mitigation, so the flagship `route_map` example is only correct if its author knows to run a republish timer | should-fix (docs) | **DEFERRED.** Guardian upheld the original decline of the `retain` kind. Residual gap sits on the example used to justify making `holds.boxes` shared. Cheap half: promote zero-delivery from INFO to WARNING when the topic has a resolved publisher, and add a static-data publisher as a fifth worked example in `plugin-api.md` §2.1. |
| **13** | F-16's decline is right, but whether a startup barrier exists is unspecified, and `unavailable()`-before-READY's documented consequence depends on it | note | **DEFERRED.** No API change needed; the contract must say which. §4.4.2 and §5.4.1 currently imply opposite answers. |
| **14** | A class-level override of a reserved name is silently ignored | note | **DEFERRED.** Inverse of S22 and quieter — the instance binding shadows the author's method, so their override never runs. Same comparison one scope up in a check that already exists. |
| **15** | `first-party-plugins.md` was never revised, so the dogfood proof contradicts the API it validates | note | **DEFERRED — queued as a `plugins-and-config` re-run.** Still shows `as_bgr().copy()`, `finish()` for the unavailable paths, `<plugin_dir>/build/`, `np.clip` on boxes, `set_interval` fallback comments, and no `[config] keys`/`conflate`/`mode`/`unit`/`record_kind` in any of the four manifests. Decision #7's claim is only as good as the last time it was exercised. Also: `plugin-api.md` §2 says "five data members, six methods" then "Four members are data" fourteen lines later. |

## Guardian-02's answers, for the record

- **Still one uniform authoring model? YES.** "The count grew; the shape did not." No second base class, lifecycle, mode flag, or type-specific hook. Would cut nothing. The real cost is discoverability — five of eleven members serve narrow plugin classes and §2 doesn't say which — which is `docs-and-testing`'s problem.
- **§7 structurally right.** §7.3 (mandatory `requires_topology` raising at the call site) judged the best thing in the section. The choice that reads fine alone and wrong against the system was **not** among the three flagged for review: it is §7.4's thread (finding 5).
- **Verified rather than trusted:** B1, B2, B4, B5 genuinely landed. B3/#19 claimed and false → finding 1, now closed.
- **All six declines upheld**; both structural deferrals properly argued. Only S9 partially overturned (finding 12).
- **Decision #24 ruled and accepted:** one concurrently *running* `ClimbCV` per process, guard scoped to running rather than construction and released on shutdown, and a single instance is not re-runnable after `stop()`.
